// No-op error reporter. Lovable telemetry has been removed.
// Kept as a stub so existing imports continue to compile.
export function reportLovableError(_error: unknown, _context: Record<string, unknown> = {}) {
  // intentionally empty
}
